[Unit]
Description=Solaar launcher service
PartOf=graphical-session.target
After=graphical-session.target

[Service]
Type=simple
ExecStart=/usr/sbin/solaar -w hide
Slice=session.slice
Restart=on-failure

[Install]
WantedBy=graphical-session.target
